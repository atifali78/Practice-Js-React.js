const arr = [1, 2, "Atif", 5, { name: "Ali" }, true];
arr[arr.indexOf(1) > -1 ? arr.indexOf(1) : arrays.length] = 5;
console.log(arr);

// -----------------------------------------
// how to create object in js
// destructuring in array
// object destructuring important in js
